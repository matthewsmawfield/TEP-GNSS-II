name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Build static site
      run: |
        cd site
        node build.js
        echo "✅ Build completed successfully"
        ls -la dist/

    - name: Build code-longspan static site
      run: |
        cd site-code-longspan
        node build.js
        mkdir -p ../site/dist/code-longspan
        cp -R dist/* ../site/dist/code-longspan/
        echo "✅ code-longspan build completed"
        ls -la ../site/dist/code-longspan

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './site/dist'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4